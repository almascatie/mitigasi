import * as maplibregl from "maplibre-gl";
const imagery = "https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}";
export function createMap(container, data) { const objects = [...data.routes, ...data.points, ...data.obstacles].filter((item) => item.visible !== false); const coordinate = objects[0]?.coordinates?.[0] || objects[0]?.coordinate || (data.location && [data.location.longitude, data.location.latitude]) || [117, -2]; return new maplibregl.Map({ container, center: coordinate, zoom: objects.length ? 13 : data.location ? 14 : 5, attributionControl: true, doubleClickZoom: false, dragRotate: false, pitchWithRotate: false, style: { version: 8, sources: { imagery: { type: "raster", tiles: [imagery], tileSize: 256, attribution: "© Esri, Maxar, Earthstar Geographics" } }, layers: [{ id: "imagery", type: "raster", source: "imagery" }] } }); }
export function flyToCoordinates(map, coordinates, zoom = 15) { if (map && coordinates) map.flyTo({ center: coordinates, zoom, essential: true }); }
