const items = [["map", "⌖", "Peta"], ["routes", "↗", "Rute Saya"], ["points", "●", "Titik Saya"], ["bag", "□", "Tas Bencana"]];
export default function Sidebar({ tab, setTab, counts, onLocation }) { return <aside className="sidebar"><div className="brand"><b>M</b><div><strong>MITIGASI</strong><small>Peta mitigasi kolaboratif</small></div></div><nav>{items.map(([key, icon, label]) => <button className={tab === key ? "active" : ""} key={key} onClick={() => setTab(key)}><span>{icon}</span>{label}{counts[key] !== undefined && <em>{counts[key]}</em>}</button>)}</nav><button className="location-button" onClick={onLocation}>⌖ Pilih lokasi manual<small>GPS tersedia di toolbar</small></button><footer>Data pribadi tersimpan lokal di perangkat.</footer></aside>; }
