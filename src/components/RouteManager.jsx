export default function RouteManager({ routes, selectedRouteId, onCreate, onSelect, onEdit, onToggle, onDelete }) { return <section><span className="eyebrow">RUTE SAYA</span><h2>Semua rute</h2><p>Setiap rute berdiri sendiri dan digambar manual.</p><button className="primary-button wide" onClick={onCreate}>＋ Buat rute</button>{routes.length ? <div className="card-list">{routes.map((route, index) => <article className={`list-card ${route.id === selectedRouteId ? "selected" : ""}`} key={route.id} onClick={() => onSelect(route)}><b><i>━</i> {route.name || `Rute ${index + 1}`}</b><small>{route.geometry.coordinates.length} titik · {route.status}</small><div><button onClick={(event) => { event.stopPropagation(); onToggle(route); }}>{route.visible === false ? "Tampilkan" : "Sembunyikan"}</button><button onClick={(event) => { event.stopPropagation(); onEdit(route); }}>Edit</button><button className="danger" onClick={(event) => { event.stopPropagation(); if (window.confirm(`Hapus ${route.name}?`)) onDelete(route); }}>Hapus</button></div></article>)}</div> : <p className="empty">Belum ada rute. Buat rute pertama langsung di peta.</p>}</section>; }
