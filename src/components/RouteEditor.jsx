export default function RouteEditor({ draft, setDraft, mode, onDraw, onPoint, onObstacle, onSave, onCancel }) { const coordinates = draft.geometry.coordinates; return <section className="editor-panel"><span className="eyebrow">{draft.id ? "EDIT RUTE" : "RUTE DRAF"}</span><h2>{draft.id ? "Edit rute" : "Buat rute"}</h2><label>Nama rute<input value={draft.name} onChange={(event) => setDraft((value) => ({ ...value, name: event.target.value }))} /></label><label>Deskripsi<textarea value={draft.description} onChange={(event) => setDraft((value) => ({ ...value, description: event.target.value }))} placeholder="Opsional" /></label><p className="draw-status"><b>{coordinates.length} titik jalur</b> · {mode.includes("route") ? "Klik peta untuk menambah titik." : "Pilih lokasi di peta."}</p><div className="tool-grid"><button className={mode.includes("route") ? "active" : ""} onClick={onDraw}>＋ Tambah titik</button><button onClick={onPoint}>● Tambah titik info</button><button onClick={onObstacle}>⚠ Tambah hambatan</button><button disabled={!coordinates.length} onClick={() => setDraft((value) => ({ ...value, geometry: { ...value.geometry, coordinates: value.geometry.coordinates.slice(0, -1) } }))}>↶ Hapus terakhir</button></div><small>Klik kanan marker bernomor untuk menghapusnya. Tarik marker untuk menyesuaikan jalur.</small><div className="actions"><button className="secondary-button" onClick={onCancel}>Batal</button><button className="primary-button" onClick={onSave}>Simpan rute</button></div></section>; }
